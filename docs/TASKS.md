# PL/0 编译原理课设任务表

本表记录项目最终完成情况。当前列出的任务均已完成。

## 分工建议

- 成员 A：项目负责人，负责总体架构、命令行入口、构建与集成。
- 成员 B：词法和语法负责人，负责 Token、Lexer、递归下降 Parser。
- 成员 C：语义和中间代码负责人，负责符号表、类型/作用域检查、四元式生成。
- 成员 D：测试和文档负责人，负责测试用例、错误样例、项目文档和演示材料。

## 必做任务

| 编号 | 任务 | 模块 | 状态 | 负责人建议 | 验收说明 |
| --- | --- | --- | --- | --- | --- |
| T01 | 项目骨架确认与目录规范 | 工程结构 | 已完成 | 成员 A | 已建立 `src/`、`tests/`、`docs/`、`cmake-build/`，入口文件和模块边界清晰。 |
| T02 | 环境检查 | 工程环境 | 已完成 | 成员 A | 已确认 `g++`、`flex`、`bison` 可用；已通过 Homebrew 安装 `cmake`，项目统一使用 CMake 构建。 |
| T03 | 文档初始化 | 文档 | 已完成 | 成员 D | 已建立任务表、中文使用说明和代码学习笔记。 |
| T04 | 命令行接口设计与实现 | CLI | 已完成 | 成员 A | 已支持 `lex`、`parse-ll`、`parse-lr`、`sem-ll`、`sem-lr`、`target`、`run`、`lr-dot`、`ir-dot`。 |
| T05 | 词法分析器 | Lexer | 已完成 | 成员 B | 已识别保留字、标识符、整数、运算符、界符、单行/多行注释，并能定位非法字符、非法单词、超长标识符和整数。 |
| T06 | 递归下降语法分析器 | Parser | 已完成 | 成员 B | 已按 PL/0 文法完成程序、分程序、声明、语句、条件、表达式等分析。 |
| T06B | SLR 自底向上语法分析器 | Parser | 已完成 | 成员 B | 已自动构造 FIRST/FOLLOW、LR(0) 项目集、ACTION/GOTO 表，并输出移进/归约过程。 |
| T07 | 符号表设计 | Semantic | 已完成 | 成员 C | 已支持常量、变量、过程登记，记录层次和值，并按声明顺序输出。 |
| T08 | 语义检查 | Semantic | 已完成 | 成员 C | 已检查重复声明、未声明标识符、常量/过程不可赋值、`read` 目标、`call` 目标和表达式中过程名使用。 |
| T09 | 四元式中间代码生成 | IR | 已完成 | 成员 C | 已为声明、表达式、赋值、读写、条件、循环、过程调用生成四元式并输出。 |
| T10 | 错误处理和恢复 | Diagnostics | 已完成 | 成员 B、成员 C | 已能报告词法、语法、语义错误行号，并在错误样例中继续输出有效诊断。 |
| T11 | 基础测试用例 | Tests | 已完成 | 成员 D | 已覆盖合法程序、词法错误、语法错误、语义错误和若干边界情况。 |
| T12 | 集成测试和演示样例 | Tests/Docs | 已完成 | 成员 A、成员 D | 已提供 CMake `check`/CTest 和 `tests/run_all.sh`，覆盖基础正确/错误样例和完整运行链路。 |
| T12B | Lex/Yacc 入门实验 | Labs | 已完成 | 成员 D | 已完成字符频率统计、单词/数字/符号识别、加法乘法计算器三个实验及运行说明。 |
| T12C | 可视化加分项 | Tools/Docs | 已完成 | 成员 B、成员 C | 已实现 SLR 项目集状态图和四元式控制流图 DOT 导出。 |
| T12D | 目标代码和解释执行 | Backend/Runtime | 已完成 | 成员 A、成员 C | 已实现四元式到伪目标代码生成，并可通过 `run` 命令查看程序运行结果。 |
| T13 | 代码整理与最终验收 | Release | 已完成 | 全员 | 源码可编译，`tests/run_all.sh` 当前 12/12 通过，README、任务表和演示样例已整理。 |

## 可选加分任务

| 编号 | 任务 | 模块 | 状态 | 负责人建议 | 验收说明 |
| --- | --- | --- | --- | --- | --- |
| B01 | LR 项目集和分析表自动构造 | Parser | 已完成 | 成员 B | SLR 模块已自动构造 LR(0) 项目集、ACTION/GOTO 表并输出分析步骤。 |
| B02 | SLR 状态转换图可视化 | Parser/Tools | 已完成 | 成员 B、成员 D | `lr-dot` 可导出 LR(0) 项目集状态和状态转换边。 |
| B03 | 四元式控制流图可视化 | IR/Tools | 已完成 | 成员 C、成员 D | `ir-dot` 可导出四元式顺序边、条件跳转边和无条件跳转边。 |
| B05 | 伪目标代码解释执行 | Backend/Runtime | 已完成 | 成员 C | `run` 命令可解释执行由四元式翻译得到的伪目标代码，验证程序运行闭环。 |
| B06 | 自动化测试脚本 | Tests | 已完成 | 成员 D | 已提供 `tests/run_all.sh`，输出 PASS/FAIL 和汇总结果。 |
| B08 | 目标代码或栈式虚拟机 | Backend | 已完成 | 成员 A、成员 C | `target` 可输出伪目标代码，`run` 可解释执行并展示运行结果。 |

## 最终收尾状态

- 当前主线功能已经收尾：词法分析、递归下降语法分析、SLR 语法分析、语义检查、四元式生成、伪目标代码生成、解释执行、DOT 可视化、Lex/Yacc 实验和测试脚本均已完成。
- 当前自动化验收结果：`tests/run_all.sh` 输出 `Summary: 12/12 passed`。
- 项目材料已经整理完成，可直接用于提交验收和答辩演示。
